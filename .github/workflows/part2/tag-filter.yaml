name: tag-filter
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+' # v1.0.0 or v2.2.1, not v1.0 or 1.0.0

jobs:
  tag-filter:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo hello world!
