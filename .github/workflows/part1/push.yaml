name: push-workflow_part1
on: push

jobs:
  push-job:
    runs-on: ubuntu-latest
    steps:
    - name: stpe1
      run: echo hello word
    - name: step2
      run: |
        echo ${{ github.event_name }}
        echo ${{ github.sha }}
        echo ${{ github.ref }}
        echo "This is a GitHub Action"